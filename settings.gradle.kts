pluginManagement {
    repositories {
        maven("https://mvn.7c7.icu") {
            name = "7c7 Maven"  // for Forge-Init-Injector
        }
        gradlePluginPortal()
    }
}

rootProject.name = "pridespecial-reborn"