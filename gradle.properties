mod_version=1.0.1

mr_version_name_template=PrideSpecial Reborn %s
mr_changelog=Fixed a wrong predicate for `equals()` in `BlankFlagShapeFactory`
# release, beta, alpha
mr_version_type=release
mr_supported_loaders=fabric,neoforge
